<catel:DataWindow x:Class="Catel.Examples.WPF.Analytics.Views.ProvideAnalyticsView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				  xmlns:catel="http://catel.codeplex.com"
                  xmlns:ViewModels="clr-namespace:Catel.Examples.WPF.Analytics.ViewModels"
                  ShowInTaskbar="True" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    
    <!-- Resources -->
    <catel:DataWindow.Resources>

    </catel:DataWindow.Resources>

    <!-- Content -->
    <catel:StackGrid>
        <catel:StackGrid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </catel:StackGrid.RowDefinitions>

        <catel:StackGrid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MinWidth="250" />
        </catel:StackGrid.ColumnDefinitions>

        <Label Content="Google Analytics API key" />
        <TextBox Text="{Binding ApiKey, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
    </catel:StackGrid>
</catel:DataWindow>
